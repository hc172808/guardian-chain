Implement a modular GYDS Program Library (GPL) inspired by Solana and its SPL Token architecture. GPL must include a centralized Token Program (GYDS-20), Accounts system, Staking module, System module, and a lightweight Smart Contract VM. Tokens must not be separate contracts; instead, use a single shared token engine. Implement the smart contract system so any registered account can deploy and interact with contracts through controlled, sandboxed execution (users are not automatically contracts). Provide full Go implementation, persistent storage using LevelDB or RocksDB, deterministic state transitions, snapshot and fast state sync support. Implement Accounts & Security with Ed25519 cryptography, address validation, and nonce-based replay protection.Build a full Transaction System with typed transactions (transfer, mint, contract call, staking), fee mechanism (base + priority), and mempool. Integrate with consensus including validator-based staking, block validation, and execution pipeline hooked into GPL. Add a complete Event & Logging system emitting indexed logs for all state changes. Provide RPC & API layer with JSON-RPC, REST, and WebSocket support for real-time updates. Implement a deterministic, gas-metered Smart Contract VM with upgradeable design. Add Permissions & Governance with role-based access (admin, validator, user), token mint authority, and upgradeable modules. Build Networking with P2P gossip, node discovery, and secure communication compatible with WireGuard. Ensure Extensibility via plugin/module system supporting future NFT standard (ERC-721-like), DeFi modules (DEX, lending), and governance. Provide Tooling including CLI, developer SDK, and full wallet/explorer API compatibility. Design all components (node, RPC, explorer, indexer, wallet backend) to run on separate servers with distributed deployment support. Ensure support for both full nodes and lite nodes with HTTPS RPC access. Maintain clean architecture, separation of concerns, and production-ready code structure and after go to/Defi  and test all in there after log it to todo file and after fix them